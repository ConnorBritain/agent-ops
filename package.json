{
  "name": "agent-ops",
  "version": "0.1.0",
  "private": true,
  "description": "A provider-neutral, policy-governed operating architecture for bounded coding-agent work.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.18.0",
    "pnpm": ">=11.9.0"
  },
  "scripts": {
    "generate:specs": "node tools/generate-public-specs.mjs",
    "generate:traceability": "node tools/generate-traceability.mjs",
    "check:specs": "node tools/generate-public-specs.mjs --check",
    "check:traceability": "node tools/generate-traceability.mjs --check",
    "check:governance": "node --test tools/public-data-guard.test.mjs && node tools/validate-governance.mjs",
    "check:service-packaging": "node --test tools/service-packaging.test.mjs",
    "check:roadmap-adapter": "node --test tools/roadmap-adapter.test.mjs && pnpm --filter @agent-ops/contracts test && pnpm --filter @agent-ops/adapters test",
    "check:print-provider": "node --test tools/print-provider.test.mjs && pnpm --filter @agent-ops/provider-sdk test && pnpm --filter @agent-ops/provider-print test",
    "check:codex-app-server-provider-spike": "node --test tools/codex-app-server-provider-spike.test.mjs && pnpm --filter @agent-ops/test-kit test",
    "check:codex-app-server-provider": "node --test tools/codex-app-server-provider.test.mjs && pnpm --filter @agent-ops/provider-codex-app-server test",
    "check:coordinator-runtime": "node --test tools/coordinator-runtime.test.mjs && pnpm --filter @agent-ops/coordinator test",
    "check:supabase": "node tools/validate-supabase.mjs",
    "roadmap:validate": "roadmap validate",
    "roadmap:render": "roadmap render",
    "typecheck": "pnpm -r --if-present typecheck",
    "test:unit": "pnpm -r --if-present test",
    "test:db": "supabase test db supabase/tests --local",
    "db:start:minimal": "supabase start --exclude gotrue,realtime,storage-api,imgproxy,kong,mailpit,postgrest,postgres-meta,studio,edge-runtime,logflare,vector,supavisor",
    "db:lint": "supabase db lint --local --schema agentops,agentops_audit,agentops_private --level warning --fail-on error",
    "validate": "pnpm run check:specs && pnpm run check:traceability && pnpm run check:governance && pnpm run check:service-packaging && pnpm run check:roadmap-adapter && pnpm run check:print-provider && pnpm run check:codex-app-server-provider-spike && pnpm run check:codex-app-server-provider && pnpm run check:coordinator-runtime && pnpm run check:supabase && pnpm run typecheck && pnpm run test:unit && pnpm run roadmap:validate",
    "test": "pnpm run test:unit"
  },
  "devDependencies": {
    "@connorbritain/roadmap": "0.4.0",
    "@types/node": "22.20.1",
    "typescript": "7.0.2"
  }
}
