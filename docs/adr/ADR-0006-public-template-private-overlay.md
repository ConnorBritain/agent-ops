# ADR-0006: Public template and private overlay separation

Status: accepted

The public repository contains generalized architecture, generic examples, and secret-safe references. A separate private overlay contains real fleet, relay, network, account, credential-reference, and deployment information. Private changes never flow upstream automatically.
